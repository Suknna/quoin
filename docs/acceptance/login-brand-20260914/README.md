# 登录页品牌展示区验收（2026-09-14）

## 范围与运行环境

- 以 `53bb2ede22991448d956f705ae819183cb93cd87` 为基线，将认证布局右侧的默认占位图替换为 Quoin 品牌展示区。
- 保留左侧表单、原双栏布局和认证逻辑；登录与首次改密共用展示区。
- 展示区包含石墨色背景、中文产品定位和静态证据关联示意。示意图为本次本地编写的 SVG，复用已安装的 Lucide 图标；不是实际调查记录、业务数据或运行状态，不新增依赖或外部素材。
- 使用 `pnpm --dir web dev` 启动 `http://127.0.0.1:5175/`，在实际 Chromium 浏览器内通过现有“开发预览 · 模拟数据”控件切换场景。
- 全部截图来自本地 MSW 模拟预览，未访问真实部署、使用真实凭据或执行实际改密；不构成生产认证验收。截图右下角的模拟预览按钮不会进入生产构建。

## 操作与截图

1. 打开本地预览，点击“开发预览 · 模拟数据”，在“场景”中选择“登录 — 未登录状态”。
2. 检查 1280×720、1920×1080 和 1024×768 的双栏布局；检查 1023×768 时展示区从页面及可访问树中隐藏。
3. 在 390×844 检查手机端，确认保留品牌、用户名、密码和登录按钮，页面无横向溢出。
4. 从用户名输入框按 Tab 到密码，再按 Tab 到登录按钮；两次焦点检查均通过，品牌区域不占用键盘焦点。
5. 用预览控件切换“首次改密 — 需要设置首次密码”，检查 1440×900 和 390×844 下的共用布局、表单及退出登录入口。
6. 在首次改密页点击“退出登录”，确认返回完整登录页；随后通过开发预览控件保存“登录”场景，并恢复 1440×900 桌面尺寸供查看。

| 场景 | 尺寸（CSS px） | 证据 |
| --- | --- | --- |
| 登录桌面 | 1280×720 | [截图](./login-desktop-1280.png) |
| 最终预览 | 1440×900 | [截图](./login-desktop-1440.png) |
| 登录宽屏 | 1920×1080 | [截图](./login-wide-1920.png) |
| 双栏最小断点 | 1024×768 | [截图](./login-breakpoint-1024.png) |
| 登录手机端 | 390×844 | [截图](./login-mobile-390.png) |
| 首次改密桌面 | 1440×900 | [截图](./password-change-desktop-1440.png) |
| 首次改密手机端 | 390×844 | [截图](./password-change-mobile-390.png) |

## UI 验收清单

依据 [前端 UI 验收规范](../../frontend/ui-acceptance.md)：

- [x] 标题层、内容层、操作层可以区分。认证页保留单一主标题与黑色主按钮，右侧文案与低对比度静态示意没有额外操作入口。
- [x] 鼠标检查可交互入口。输入框、登录/保存按钮、退出登录和开发预览控件可辨认；示意图不使用按钮、链接或点击手势。
- [x] 宽屏有明确限宽。1920 CSS px 下左右各 960 px，品牌内容最大 512 px，示意图最大 448 px，表单仍最大 320 px。实测文档宽度和滚动宽度均为 1920 px；手机端均为 390 px。
- [x] 同级标题排版一致。两个认证状态复用同一展示区组件与标题样式，不增加页面内的同级标题变体。
- 不适用：0 / 1 / 50 条数据以及动态超长内容。此次右侧只有固定文案与装饰性示意，不读取业务列表；没有新增数据列表或动态文本。
- 不适用：新增弹窗。此次没有新增弹窗或交互式展示组件。

## 自动化与审阅

- 登录页品牌区域可访问性断言先执行并按预期失败（找不到“关于 Quoin”区域），实现后通过。
- 在现有认证测试中覆盖登录与首次改密的共用品牌区域，认证行为回归通过。
- `pnpm --dir web typecheck`：通过。
- `pnpm --dir web lint`：通过。
- `pnpm --dir web build`：通过。
- `pnpm --dir web test`：31 个文件、213 个测试全部通过。
- 独立代码规范审阅：0 个需要修复的问题。
- 独立需求符合性审阅：0 个需要修复的问题。
- 未运行真实后端认证或部署端到端测试；未部署、提交或推送。

## 文案精简复验

以上截图及完整验收记录为首版。本轮根据用户选中的文案区和底部段落进行精简：

- 顶部改为 `quoin | 运维工作台`。
- 主文案逐字使用用户提供的 `让每次一判断，都有据可寻。`。
- 删除说明段落及底部“告警调查 / 定期巡检 / 知识沉淀”；保留静态证据示意图，认证流程与小屏隐藏规则不变。
- 通过现有开发预览热更新检查文案与可访问树，确认被删除内容不再出现。
- 实际浏览器复查 [1440×900](./login-simplified-1440.png) 与 [1920×1080](./login-simplified-1920.png)：标题、操作和图形层次清楚，无多余底部文案；内容限宽保持不变。无新增可点击区域、同级标题或动态数据列表，数据边界项目仍不适用。
- 更新登录和首次改密断言；本轮 `App.test.tsx` 的 14 个测试、类型检查、lint 和 `git diff --check` 均通过。未重复执行完整测试套件和生产构建；上节 213 个测试及构建结果属于首版验收。

## 入场动画复验

- 在精简文案基础上增加单次入场动画：品牌文字、标题淡入并向上移动 8 px；证据节点按 140 ms 间隔出现，连线逐步绘制，最后展示调查记录。完整时序约 1.65 秒，无循环、计时器或持久化的“已播放”标记。
- 复用 `tw-animate-css` 的入场关键帧，仅连线使用局部 CSS 关键帧。没有新增依赖；SVG 定位保留在外层分组，避免入场变换覆盖原有坐标。
- 动画只在 `prefers-reduced-motion: no-preference` 下生效，默认内容可见。左侧表单不参与动画，首次改密共用同一品牌区。

### 浏览器操作与结果

1. 刷新当前登录预览，等待真实登录标题出现后检查动画：11 个动画实例正在播放，首次采样约 16.7 ms，全部只播放一次；用户名已自动获得焦点，表单可立即输入。
2. 在 1920×1080 刷新复查约 800 ms 的中段：标题已完成、节点与连线按顺序出现，调查记录尚未出现；没有横向溢出，正文限宽不变。
3. 等待结束后检查：活动动画数为 0，标题透明度为 1，全部内容归位。标题、内容与操作层次仍清楚，没有新增交互区域或同级标题变体；动态数据边界项目不适用。
4. 从 `about:blank` 正常导航进入本地首页，等待登录页挂载，再次观察到 11 个动画实例从约 16.8 ms 开始播放，验证不是仅首次热更新才播放。
5. 390×844 下装饰区隐藏，活动动画为 0，页面滚动宽度等于视口宽度 390 px。
6. 在动画结束后输入用户名，确认输入可用且没有重新触发动画；未提交登录请求或使用真实凭据。
7. 当前浏览器系统偏好为 `reduce=false`，没有修改操作系统设置。通过临时关闭对应 CSS 媒体规则并立即恢复，验证无动画样式时标题 `animation-name: none`、透明度 1、变换为 none；这是无动画回退检查，不冒充真实系统偏好切换验收。

### 证据

- [完整入场效果录屏（1440×900）](./login-entrance.webm)：通过从手机尺寸恢复桌面尺寸触发真实 CSS 入场，30 fps、4 秒。已抽取 0.25 / 0.8 / 1.8 秒帧进行视觉检查；这段不是刷新操作的录屏。
- [刷新起始截图](./animation-start.png)
- [刷新中段截图（1920×1080）](./animation-middle-1920.png)
- [完成后的静止状态（1440×900）](./animation-finished-1440.png)
- [手机端无装饰动画（390×844）](./animation-mobile-390.png)
- 第一次尝试在录制中刷新导致录制任务取消，未生成刷新录像；刷新行为由上述实时动画状态和截图验证，不宣称该取消任务已成功。

本轮 `pnpm --dir web build`（含类型检查）、`pnpm --dir web lint`、`git diff --check` 及完整前端测试通过：31 个文件、213 个测试。未修改认证业务逻辑，未部署、提交或推送。
