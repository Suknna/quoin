# 品牌素材接入验收

## 范围

将 material/ready 已确认素材接入登录页横向标志、右侧品牌插画、工作台侧栏及 HTML 图标声明。实际部署资源在 web/public/brand，处理与 SHA-256 记录见 ../../frontend/brand/runtime-assets.yaml。未部署、未提交或推送。

## 实际浏览器检查

本地 `pnpm --dir web dev`，http://127.0.0.1:5175/，使用已有 MSW 开发预览场景，无真实凭据。

- 登录页 1920×1080：[截图](login-wide.png)。品牌、标题、插画和登录操作层次清楚，品牌内容与插画限宽，图片无拉伸、重叠或破图。右侧文案保持不变，图片上的标签由 HTML 排版。
- 登录页 390×844：[截图](login-mobile.png)。右侧隐藏，顶部组合标志完整，表单未被装饰影响。
- 通过“开发预览 · 模拟数据”选择管理员场景，工作台 1440×900：[截图](workspace.png)。侧栏顶部独立标志可辨认，原 Quoin 导航按钮语义与点击行为保留。
- 新增内容没有数据列表，0/1/50 条及超长动态内容不适用；没有新增弹窗。现有输入和按钮可辨认，未新增伪交互元素。
- 同级标题未新增排版变体。动画沿用现有 CSS 与 reduced-motion 规则；本轮截图检查静止状态，未重复录制动画。深色素材切换已接线，但本轮未在浏览器中切换全站暗色模式。
- favicon 已添加 HTML 声明并随生产构建输出；未把页面截图当作浏览器标签图标视觉验证。

## 自动化

执行通过：完整前端测试 32 个文件、219 个测试；lint 零警告；生产 build（含 TypeScript 检查）；git diff --check。
