# Kubernetes 真实 GUI 端到端验收

## 环境与验证边界

- 入口：<https://192.168.1.200:30447/>
- 命名空间：`quoin-gui-mall-20260910`。
- 业务：`local-inspection-demo`（本地商城巡检），计划 `mall-health`，时区 `Asia/Shanghai`，每五分钟执行。
- 模型：真实 `deepseek-v4-flash`；指标连接 `mall-live-prometheus` 指向本地 Prometheus，采集 mall-lab Java、MySQL、Redis。
- 凭据：`.artifacts/k8s-gui-mall-20260910/credentials.yaml`，不在本文复制。
- 所有产品动作通过内置浏览器操作。仅上游刺激使用真实独立 Alertmanager API；没有直接向 Quoin 写入验收告警或伪造模型结果。

## 已取得的业务链路证据

| 流程 | 实际操作与结果 |
| --- | --- |
| YAML 手动巡检 | 页面创建、验证、发布商城声明；点击创建巡检。Run 6 于 08:37:58 手动触发，08:38:04 自动生成报告 v1，证据 20–22，三项采证成功。未人工重新分析。 |
| 定时巡检 | Run 5 于 08:35:00 按计划触发，08:35:08 自动生成报告 v1，证据 17–19；后续 Run 13 同样成功。不是将采证完成冒充报告成功。 |
| 报告与证据 | Run 13 的 Markdown 标题、表格、滚动区已验证；09:20:01 点击“已采纳”反馈保存成功。证据 41 正确显示 `plinth_promql` 与真实指标连接，原始 `up` 值为 1。 |
| AI SRE | 页面选择业务后创建 Investigation 2；修复后点击重试，Attempt 167 于 11:34:00 成功，证据 137–141。页面发送第二轮追问，Attempt 172 于 11:39:21 成功，引用前轮采样值并说明业务健康推断边界。重新打开对话后回复仍可读，证据 137 可点击查看原查询及执行时间。 |
| 告警接收与分析 | 页面创建 `mall-gui-alertmanager`；通过独立真实 Alertmanager 发送 `MallAcceptanceVerified`，11:53:45 页面收到并正确显示受控测试说明及业务归属。点击“AI 分析”后已完成，产生证据 157–174；结论引用上游原文，区分受控测试与真实事故，并说明瞬时指标的覆盖限制。 |

时间均为 2026-09-11 的页面本地时间，除非显式标记 UTC。模型结论不是人工签署的故障证明；个别推断或时间换算仍需运维人员核实。

## 安全迁移与历史保留

直接业务对话此前被旧 SQL 规则要求来源告警而拒绝。修复使用明确的业务配置、标签契约和指标连接授权；存在告警来源时仍校验归属，没有伪造来源或取消隔离约束。

迁移过程：

1. 页面创建手动备份 #2，再执行准备升级及安全清单。
2. 升级备份 #3 验证成功，维护版本 2，`quoin_upgrade_prepared=1`。
3. 停止本次命名空间的执行组件和 Quoin，保留所有 PVC。
4. 旧镜像 `migrate preflight` 成功；另保存含匹配私钥及运行时状态的私有冷备份。
5. 新镜像 `migrate` 在单一事务中完成结构、摘要、迁移账本、维护退出及审计，迁移历史为 1。
6. 服务恢复 Ready。旧失败分析、报告、对话及证据未删除。

冷备份包含私密数据，仅限本机受控使用，不可公开上传。前期误删事故仍保留在 `docs/demo-execution-incident.md`，本次新环境不代表原事故数据恢复。

## 演示步骤

1. 登录后进入“运维中心 → 告警列表”，找到 `MallAcceptanceVerified`；测试事件自动恢复后从历史告警打开，查看概览、AI 分析及证据。
2. 进入“AI SRE → 对话”，打开“商城真实巡检验收”查看两轮回复；新建对话时显式选择“本地商城巡检”，再输入巡检问题。
3. 进入“运维中心 → 巡检”，选择商城，打开历史 Run 或点击“创建巡检”；采证与分析状态均成功后查看报告。定时计划每五分钟触发。
4. 知识库、Kubernetes 接入及浏览器巡检显示开发中，不作为本次交付能力演示。

## 证据位置

`.artifacts/k8s-gui-mall-20260910/evidence/gui/` 保存成功截图及修复前失败截图；失败证据不应当作当前通过证据。详细进度与版本记录位于同级 YAML 文件。

## 自动化回归

已执行全量 `go test ./...`、`go vet ./...`，前端 27 个测试文件共 153 项通过，类型检查与 lint 通过。最终聊天布局微调另有针对性检查；自动化回归不代替上述真实页面证据。
