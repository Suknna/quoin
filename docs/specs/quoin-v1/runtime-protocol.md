# Quoin v1 Runtime 协议

**状态：Draft**

**Non-normative：** 本文件（CATEGORY=`RUNTIME`）承载 Quoin、Plinth、Lintel、Stele 四组件之间的 gRPC 协议语义：身份、版本握手、控制流、任务派发与调和、lease/fencing、Plinth 模型/工具调用、浏览器会话隧道、Artifact 上传与按需读取、连接凭据 grant、token 注册与轮换、吊销与告警接入。机器可表达的 service、RPC、message、stream 与 wire-level 枚举由 [`contracts/runtime.proto`](contracts/runtime.proto)（package `quoin.runtime.v1`，SPEC-VERSION-002）唯一拥有（SPEC-AUTHORITY-001/002）；本文件只通过相对路径与稳定符号引用它，不复制字段清单。持久化权威为 `persistence.md` 与 `contracts/sql/schema.sql`；前端→Quoin 的 HTTP 面（含 reveal 流程）为 `http-api.md` 与 `contracts/openapi.yaml`（HTTP-SCOPE-003）。

## 1. 范围与权威

- **RUNTIME-SCOPE-001 —** 四组件 gRPC 的 service、RPC、message、stream 方向与枚举值 **MUST** 只由 `contracts/runtime.proto`（`quoin.runtime.v1`）定义；本文件 **MUST NOT** 声明 proto 中不存在的服务、消息、字段或枚举值（SPEC-AUTHORITY-001/003）。
- **RUNTIME-SCOPE-002 —** 本文件 **MUST** 只拥有无法由 proto 表达的跨流语义：握手顺序、身份裁决、lease/fencing、调和、提交顺序裁决、故障与恢复；不重复 proto 已表达的形状。（来源：README「权威源与事实所有权」、Issue #8）
- **RUNTIME-SCOPE-003 —** locator（attempt、operation、artifact、source、credential 等）在 proto 中 **MUST** 使用 `int64`，与 SQLite INTEGER 及 HTTP 十进制字符串表示一致（DATA-IDENT-002）；`0` 表示无该 locator，边界校验见 RUNTIME-VALIDATION-004。
- **RUNTIME-SCOPE-004 —** 数值型协议调优参数（heartbeat 周期、lease 时长、上传 chunk 大小、流队列深度、重附着宽限期、轮换窗口）**MUST** 由原型测量后作为同一 Release 的内部常量冻结并由四组件共享，**MUST NOT** 成为 Helm values、Compose 输入、Admin 设置或组件自由组合配置；本文件与 proto 只拥有字段类型与语义（Timestamp/Duration/uint64/uint32），具体数值由版本化 release source 锁拥有。（来源：CONTEXT「执行尝试」「健康语义」、Issue #11 研究结论、Issue #17 Q17.1/Q17.22）

## 2. 身份与认证

- **RUNTIME-AUTH-001 —** Plinth、Lintel **MUST** 主动出站拨号 Quoin gRPC server（维持只出站部署，Q2 冻结）；TLS **MUST** 只承担服务端身份与传输保护，**MUST NOT** 建立 mTLS 客户端身份。（来源：CONTEXT「服务身份」、架构记忆 #5860）
- **RUNTIME-AUTH-002 —** 长期服务 token **MUST** 经 gRPC metadata（`authorization: Bearer <token文本>`）认证，**MUST NOT** 出现在任何消息体（唯一受保护例外：`RegisterRuntimeResponse.long_term_token` 与 `IssueToken.token` 是下发路径，只经已认证/已注册 TLS 通道出现一次）；raw token 只存在于 supervisor 内存、权限 `0600` 的专用持久状态卷与认证瞬间；数据库只存 digest（DATA-RUNTIME-002、CONTEXT「模型调用边界」）。
- **RUNTIME-AUTH-003 —** 连接认证裁决 **MUST** 满足：token digest 等于该 slot `runtime_slots.current_credential_id` 指向的 `runtime_credentials.token_digest`、该行 `confirmed_at` 非空且 `retired_at` 为空（DATA-RUNTIME-001/002）；`state='revoked'` 期间旧凭据一律拒绝，未被 current 指针选中的已确认历史行 **MUST NOT** 被接受。
- **RUNTIME-AUTH-004 —** 认证失败（令牌无效、slot 吊销、版本不匹配、epoch 回退）**MUST** 在握手阶段以 `HelloAck{accepted=false, reject_reason}` 拒绝并关闭流；组件 **MUST NOT** 进入 Ready。（来源：CONTEXT「服务身份」「健康语义」）
- **RUNTIME-AUTH-005 —** 连接凭据与模型供应商凭据 **MUST** 只由 supervisor 持有并注入类型化工具；Plinth/Lintel worker **MUST NOT** 读取状态卷、环境变量或任何凭据文件；`FetchCredentialGrant` 是 supervisor-only RPC（RUNTIME-GRANT-002），worker 进程不得持有该调用能力（CONTEXT「模型调用边界」、架构记忆 #5868）。
- **RUNTIME-AUTH-006 —** 全部 token 的文本编码 **MUST** 是 32 随机字节的 base64url（无填充）ASCII 文本：长期 token、一次性注册令牌与 HTTP reveal 产出的注册令牌都是同一编码；metadata 的 `authorization: Bearer` 值、0600 状态卷文件内容与 `RegisterRuntimeRequest.one_time_token`/`RegisterRuntimeResponse.long_term_token`/`IssueToken.token` 均为该文本。数据库 `runtime_credentials.token_digest`/`alert_source_credentials.digest` **MUST** 是解码后原始 32 字节的 SHA-256（`length(token_digest)=32`，DATA-RUNTIME-002）。内容 digest（`AttemptInputSnapshot.content_digest`/`ResultPayload.content_digest`/`ArtifactUploadHeader.sha256`）是原始 32 字节二进制，用 `bytes` 承载，**MUST NOT** 做文本编码。（来源：CONTEXT「服务身份」、Issue #11 第一轮审阅）

## 3. 连接与握手（`RuntimeControl.Connect`）

- **RUNTIME-CTRL-001 —** 每个 slot **MUST** 至多一条活动控制流；Quoin 在内存临界区维护连接投影，新连接（`connection_epoch` 更大）**MUST** 替换旧连接并关闭旧流，旧流上未完成的领域动作 **MUST NOT** 产生权威状态（RUNTIME-CTRL-009、RUNTIME-TASK-005/008）。控制流与 Browser/Artifact 流是不同 RPC；v1 **MAY** 共用同一 TCP/HTTP-2 channel，**MUST NOT** 强制独立 channel；若原型证明互相干扰，部署配置再拆分。（来源：CONTEXT「服务身份」、Issue #11 任务约束）
- **RUNTIME-CTRL-002 —** 连接首帧 **MUST** 为 `Hello`（slot、boot_id、connection_epoch、release_version；Lintel 另带 catalog digest/version、固定 browser capacity 与精确 Chromium revision）；Quoin **MUST** 先完成认证与握手校验再回 `HelloAck`；握手完成前任何其他消息 **MUST** 被忽略或拒绝。
- **RUNTIME-CTRL-003 —** `release_version` **MUST** 与 Quoin 严格相等（`==`），**MUST NOT** 做任何协商或前缀匹配；不匹配时 `HelloAck{reject_reason=VERSION_MISMATCH}` 且组件不 Ready（RUNTIME-VERSION-001）。
- **RUNTIME-CTRL-004 —** `boot_id` **MUST** 在每次进程启动时重新生成；`connection_epoch` 是 boot-local 计数器，**MUST** 在该 boot 的每次连接时递增（首条 >= 1）。Quoin 只在同一 `(slot, boot_id)` 内拒绝非单调（<= 该 boot 上次接受值）的连接（`EPOCH_STALE`）；新 boot 的首个 epoch 可重新从 1 开始，接受新 boot 后旧 boot 的全部流按 RUNTIME-CTRL-009 失效。（来源：Issue #11、RUNTIME-CTRL-009）
- **RUNTIME-CTRL-005 —** `Heartbeat` **MUST NOT** 改写任何持久状态：只更新内存瞬时投影，**MUST NOT** 递增 `runtime_slots.row_version`；通用 Attempt `Capacity` 只作提示。Lintel 浏览器总 slot 数由该 boot 的 `Hello.browser_capacity_slots` 冻结，连接存续期间不得改变；Quoin 是唯一 `WaitingForCapacity` 全局 FIFO 队列，manual login/Journey/Exploration 各占一 slot，authentication probe 使用同一 slot 预算。（来源：CONTEXT「健康语义」、Issue #14）
- **RUNTIME-CTRL-006 —** 每条流的每一端 **MUST** 使用单一 send loop / recv loop；发往对端的消息 **MUST** 进入有界队列，队列溢出 **MUST** 可观察（部署内部指标）且 **MUST NOT** 静默丢弃领域消息（派发、取消、结果）。
- **RUNTIME-CTRL-007 —** `GoAway` 是 Quoin 结束连接的尽力而为通知；实际关闭以 RPC 结束为准，客户端 **MUST NOT** 依赖 `GoAway` 送达；`SHUTTING_DOWN` 表示可重试的暂时不可用，`REVOKED`/`ROTATED`/`REPLACED` 表示必须按 RUNTIME-REVOKE-001 处理。
- **RUNTIME-CTRL-008 —** 物理消息重复投递 **MUST** 是幂等的：领域事实按 `attempt_id`+`boot_id`+`connection_epoch` 去重；对端不重复提交。
- **RUNTIME-CTRL-009 —** 每个 `ControlEnvelope` **MUST** 携带四个 envelope 字段（proto 强制形状，本条款定义语义）：`message_id`（当前连接内按方向单调递增且唯一，用于对 gRPC 缓冲重放/透明重试的物理去重：同 `(slot, boot, epoch, direction, message_id)` 的重复投递 **MUST** 被忽略、不重复执行）、`connection_epoch` 与 `boot_id`（除 `Hello` 外 **MUST** 等于 Quoin 当前已接受流上下文；不一致 **MUST** 丢弃且最多审计，**MUST NOT** 产生任何状态变化）、`correlation_id`（请求-响应对关联：`Hello`↔`HelloAck`、`DispatchAttempt`↔`AttemptAccept/AttemptReject`、`ReconcileRequest`↔`ReconcileReport`、`ResultProposal`↔`ResultAck`、`CancelAttempt`↔`CancelAck`、`IssueToken`↔`TokenPersisted`、`RequestBrowserSubExecution`↔`BrowserSubExecutionAck`、`BeginModelCall`↔`BeginModelCallAck`、`CompleteModelCall`↔`CompleteModelCallAck`、`BeginToolCall`↔`BeginToolCallAck`、`CompleteToolCall`↔`CompleteToolCallAck`、`ToolResultDelivery`↔`ToolResultDeliveryAck`、`StartBrowserOperation`↔`StartBrowserOperationAck`、`CompleteBrowserOperation`↔`CompleteBrowserOperationAck`、`PublishBrowserProfile`↔`PublishBrowserProfileResult`、`ProfileInventoryRequest`↔`ProfileInventoryReport`、`StopBrowserOperation`↔`StopBrowserOperationAck`（回带原子执行请求关联）；0 = 无）。**所有**会写权威状态的消息（`AttemptAccept`/`AttemptReject`/`ResultProposal`/`CancelAck`/`TokenPersisted`/`ReconcileReport`/`RequestBrowserSubExecution`/`BeginModelCall`/`CompleteModelCall`/`BeginToolCall`/`CompleteToolCall`/`StartBrowserOperationAck`/`CompleteBrowserOperation`/`PublishBrowserProfileResult`/`ProfileInventoryReport`/`StopBrowserOperationAck` 等）在事务提交前 **MUST** 复核当前活动流上下文（slot+boot+epoch）与 envelope 一致；旧流迟到消息（新 epoch 已接受后到达的旧流 buffered 消息）一律按上文丢弃/审计，旧流上未完成的领域动作 **MUST NOT** 产生权威状态（RUNTIME-CTRL-001）。`Hello` 的 envelope 字段描述提议的连接身份（epoch 为提议值，可不同于当前活动流）。（来源：Issue #4 研究证据、Issue #11 第一轮审阅）
- **RUNTIME-CTRL-010 —** 仅 Lintel 的 `Hello` **MUST** 携带 Journey Catalog digest/version、`browser_capacity_slots>=1`、非空精确 `chromium_revision` 与排序去重的实际 `active_browser_operations`；Plinth 的这些字段必须为空/0。Catalog digest/version 与 Quoin 嵌入值严格相等，否则 `CATALOG_MISMATCH` 且不 Ready。Quoin/Lintel 嵌入同一 JCS bytes，禁止两侧重序列化。每个新 Lintel boot 的 `HelloAck.profile_reconcile_required=true`；握手后 Quoin 立即执行 RUNTIME-BROWSER-015 全量 profile reconcile，报告完整前 Lintel 仍不具备 Browser Operation readiness。（来源：CONTEXT「Journey Catalog」「浏览器身份」、Issue #12/#14）
- **RUNTIME-CTRL-011 —** 配置 Test Run 的机械采集（`scope_type='config_test_run'` 的 `inspection_collection` 子 Attempt）**MUST** 与巡检采集共用同一派发/结果/取消/调和路径（RUNTIME-TASK-001..012、RUNTIME-CANCEL-001..003）；Run 状态由 SQL 机器权威与 DATA-CONFIG-007 语义推进；Journey 等待浏览器 slot 时 Run 保持 `Running`，`Passed` **MUST** 只在该 Test Run 的绑定配置版本全部 check 形成 ok+Evidence 后落库；Test Run 结果与取消按 SQLite 提交顺序裁决（DATA-TX-005）。（来源：CONTEXT「业务系统配置版本」、Issue #12 决策）

## 4. 任务派发与调和

- **RUNTIME-TASK-001 —** Quoin **MUST** 先持久化 `execution_attempts`（`attempt_id` 权威）再派发：派发事务在同一事务内设置 `runtime_slot`/`boot_id`/`connection_epoch`/`lease_until`（`Assigned`）并复查目标 slot `registered` 且 current 指向已确认未退休凭据（DATA-ATTEMPT-001、DATA-RUNTIME-001）；事务提交后 **MUST** 才发送 `DispatchAttempt`。
- **RUNTIME-TASK-002 —** `DispatchAttempt` **MUST** 携带 attempt_id、attempt_type、scope_type、scope_id、可选 check_key、仅 `config_test_run` 子 Attempt 必填的 `plan_key`、`lease_deadline`（有限 lease）、完整 `AttemptInputSnapshot`（RUNTIME-TASK-011）与可选的 `requested_by_tool_call_id`；lease 时长数值为部署配置（RUNTIME-SCOPE-004）。
- **RUNTIME-TASK-003 —** Attempt 类型与 slot 的固定映射 **MUST** 与 schema CHECK 一致：`browser_exploration`/`inspection_collection` 只派发 `lintel`，`initial_analysis`/`investigation`/`inspection_analysis`/`knowledge_extraction`/`embedding`/`model_provider_probe` 只派发 `plinth`（DATA-RUNTIME-001b、DATA-ATTEMPT-001）。（来源：Issue #13、DATA-ATTEMPT-001）
- **RUNTIME-TASK-004 —** Runtime **MUST** 对每个 `DispatchAttempt` 回 `AttemptAccept` 或 `AttemptReject`：Accept 后 Quoin 写 `accepted_at` 并转 `Running`；`NO_CAPACITY` 拒绝时 Attempt **MUST** 保持 `Assigned`（绑定不可改），Quoin **MAY** 幂等重发同一 `DispatchAttempt`（同 attempt_id/boot/epoch），超过部署内部重试策略后按 RUNTIME-TASK-009 终止；`INPUT_UNSUPPORTED`/`INTERNAL` 拒绝时 Quoin **MUST** 转 `Failed`（`provider_unavailable`）。
- **RUNTIME-TASK-005 —** lease 内同 boot 重连（`Hello` 的 boot_id 相同且 epoch 更大、lease 未到期）**MUST NOT** 重派：Quoin **MUST** 发送 `ReconcileRequest`（该 slot 当前 active 集合），Runtime **MUST** 回 `ReconcileReport`（实际运行集合），双方按 attempt_id 对齐；Lintel 还必须用 `Hello`/`Heartbeat.active_browser_operations` 调和同 boot 的实际 Browser Operation 进程集合，Quoin 对已终态但仍活跃者重发 Stop，对报告不存在者写入停止确认。`Assigned` 未接受的 Attempt **MAY** 在调和后幂等重发。
- **RUNTIME-TASK-006 —** 对 `Assigned|Running` Attempt，新 boot（boot_id 不同）**MUST** 使旧 boot 绑定 Attempt 转 `Interrupted`（`lease_expired`）；slot 替换转 `Interrupted`（`replaced`），凭据吊销转 `Interrupted`（`revoked`），lease 到期无续期转 `Interrupted`（`lease_expired`）。已经提交 cancellation fence、状态为 `Cancelling` 的 Attempt 是例外：上述任一失联条件都必须按 RUNTIME-CANCEL-003 收敛为 `Cancelled`，不得改写为 `Interrupted`。（来源：CONTEXT「执行尝试」、DATA-ATTEMPT-001）
- **RUNTIME-TASK-007 —** `lease_until` **MAY** 由心跳或活动消息续期（每次 UPDATE 恰好递增 `row_version`，DATA-ATTEMPT-006）；续期判定数值为部署配置。
- **RUNTIME-TASK-008 —** 结果 **MUST** 经 `ResultProposal` 提交（attempt_id + boot_id + connection_epoch + outcome + 可选 termination_reason + `ResultPayload`）；Quoin **MUST** 按 attempt_id+boot+epoch 幂等裁决：旧 epoch、lease 已到期或 cancellation fence 已提交的迟到结果 **MUST NOT** 产生有效领域输出，只留审计（DATA-ATTEMPT-004）；`ResultAck{accepted}` 回传裁决。（来源：Issue #13、DATA-ATTEMPT-004）
- **RUNTIME-TASK-009 —** 部分 token 后失败 **MUST NOT** 成为有效输出：Attempt `Failed` + 结构化 `TerminationReason`（与 schema CHECK 枚举一致）；成功前所有引用 Artifact **MUST** 经 `ArtifactService.Upload` 上传校验提交（DATA-ATTEMPT-005、DATA-ARTIFACT-006）。
- **RUNTIME-TASK-010 —** `AttemptProgress` **MUST NOT** 成为权威状态来源：显示性进度；权威进度由 `tool_calls`/`evidence` 持久记录承担（DATA-EVIDENCE-001）。
- **RUNTIME-TASK-011 —** 任务执行输入 **MUST** 由 Quoin 根据 `attempt_input_snapshots`/`attempt_input_items` 权威引用重建，并在派发时由 `AttemptInputSnapshot` 完整承载（**MUST NOT** 只传 digest 让对端自行重建）：`schema_kind` 为八种登记值，`canonical_json` 非空且 digest 匹配，Artifact/grant 只引用已冻结非秘密事实。`inspection_collection_v1` 的 canonical JSON **MUST** 通过 [browser-execution.schema.json](contracts/schemas/browser-execution.schema.json) 的 Journey input 子 Schema；`browser_exploration_v1` **MUST** 只通过 [browser-tool.schema.json](contracts/schemas/browser-tool.schema.json) 的 `browserExplorationInput`，其中同时冻结 Browser Operation、identity revision、profile generation、catalog 与单个 Browser Tool action。不得为 Exploration Session 建立第二份输入 Schema。通过形状校验后，Quoin **MUST** 再把 input 的 attempt/operation/identity revision/profile generation/catalog digest+version/Journey 逐项与同一事务读取的 `execution_attempts`、`browser_operations` 和 Catalog 权威行精确比对；`open` 尚未创建 operation 时也必须把 catalog binding 与目标 identity current revision 精确比对。任何 ID/digest/version 交叉错配均以 `invalid_response` 拒绝，不能因为各字段分别合法而接受。对端拒绝未知 schema/digest/字段；秘密只经类型化 grant 下发。（来源：Issue #13、Issue #14、DATA-ATTEMPT-003）
- **RUNTIME-TASK-012 —** `ResultProposal.payload` 的 `schema_kind` **MUST** 与 AttemptType 封闭匹配。`inspection_collection` 只接受 `browser_journey_result_v1` 并通过 [browser-execution.schema.json](contracts/schemas/browser-execution.schema.json) 的 Journey result 子 Schema；`browser_exploration` 只接受 `browser_exploration_result_v1` 并通过 [browser-tool.schema.json](contracts/schemas/browser-tool.schema.json) result 子 Schema。其它模型/分析类型继续使用登记的封闭 schema kind。Journey success 必须携带恰一个 `primary=true` 的 structured Evidence proposal，其 `content` 是 typed output 的唯一正文；业务 gap 的 Evidence 列表必须为空，只携带封闭 gap code、与 SQL 映射一致的 terminal reason 和 detail。Quoin 校验 payload digest/schema，把 attempt/operation/probe/catalog/Journey 与冻结事实精确比对，以 operation 实际 Catalog output schema/Evidence kinds 重验 success proposals 并先提交所引 Artifact/Evidence；随后以单条 `browser_journey_results` INSERT 作为提交入口，由 SQL trigger 派生 check result、按 outcome 收口 operation，并最终把 `inspection_collection` Attempt 置 `Succeeded`（Succeeded 仅表示合法结果已提交，check 的 ok/gap 独立表达业务结论）；因此合法 Journey gap 的顶层 `ResultProposal.outcome` 仍为成功且 Attempt `termination_reason` 为空，payload 内 `terminalReason` 只收口 Browser Operation，不得成为第二个 Attempt 终止原因。相同 operation 的同 digest 重放读取该不可变 ledger 重建原 Ack，不同 digest 因 operation 唯一键作为协议冲突拒绝；operation 创建前的 `identity_busy` 结果由 check result保存 digest、无 Evidence，并原子收口未派发 Attempt。Exploration Result 同样先校验 action/observation/Artifact 闭包；`ResultPayload.artifact_ids` 只是从 payload 内全部 Artifact locator（含 `screenshotArtifactId`）机械导出的传输闭包，必须集合完全相等且不得表达额外 Artifact，语义 locator 只由对应封闭 payload 字段拥有，不形成第二权威。随后以父 `quoin_browser` Tool Call 终态 UPDATE 为提交入口，由 trigger 在同一 statement 推进子 Attempt；不得先单独提交子 Attempt 终态。未知或不匹配 payload 以 `invalid_response` 失败且不得落库；不得使用未定义补通道或不透明日志。（来源：Issue #13、Issue #14、DATA-ATTEMPT-005）

## 5. 取消与结果裁决

- **RUNTIME-CANCEL-001 —** 用户取消 **MUST** 先由 Quoin 事务提交 cancellation fence（`Queued` 直接 `Cancelled`；`Running` 先转 `Cancelling`），事务提交后 **MUST** 才发送 `CancelAttempt`（DATA-ATTEMPT-003、HTTP-COMMAND-005）。
- **RUNTIME-CANCEL-002 —** 成功结果与取消 **MUST** 按 SQLite 提交顺序裁决（DATA-TX-005）：成功先提交则取消返回已完成对象；取消先提交则迟到结果不产生有效消息/Report/Candidate，只留审计。
- **RUNTIME-CANCEL-003 —** Runtime 收到 `CancelAttempt` 停止后 **MUST** 回 `CancelAck`；Quoin 收到后转 `Cancelled`。fence 与 transport detach 并发时 **MUST NOT** 停留在 `Cancelling` 中间态：lease 到期或流关闭后 Quoin 按 RUNTIME-TASK-006 收敛终态。旧流迟到的 `CancelAck`/`AttemptAccept` 按 RUNTIME-CTRL-009 envelope fence 丢弃。

## 6. Plinth 模型与工具循环

- **RUNTIME-AGENT-001 —** Plinth **MUST** 复用 Eino v0.9.13 的 ChatModel、OpenAI-compatible adapter、Message/Tool/stream/callback 基元，自行实现最小顺序 ReAct loop；**MUST NOT** 使用带默认总轮数上限的 ChatModelAgent/Runner 或供应商托管 AgenticModel。第一版不设置 Attempt 总模型轮数、总 Tool Call 数或总时长上限；单次 provider/外部 API 调用仍使用内部 deadline 与严格有界重试。（来源：Issue #13 Q13.1 A）
- **RUNTIME-AGENT-002 —** 每个物理 provider 请求前，Plinth **MUST** 先发送 `BeginModelCall`。Quoin 事务复核 Running Attempt、固定 agent/model capability、同一 `call_seq/retry_seq` 未存在、输入项与 digest 闭合后，创建 `model_calls(status=running)` 并绑定派发前已经冻结的模型 grant，提交后才回 `BeginModelCallAck{accepted=true}`。Plinth 只有收到 accepted Ack 并通过 supervisor-only `FetchCredentialGrant` 取得该 grant 后才能调用 provider；worker 永不看到 grant/connection/credential。（来源：DATA-TX-017）
- **RUNTIME-AGENT-003 —** `BeginModelCall.input_items` **MUST** 精确表达本次按序模型输入的权威来源与 digest，`rendered_request_digest` 必须是 supervisor 对 worker 实际请求规范化后的 SHA-256；Quoin **MUST** 复算来源闭合并持久化该实际请求 digest，使 renderer/version/source 可独立重建核对。上下文超预算时只允许从最旧端淘汰完整对话回合，Tool Call/Result 与所属 assistant turn 共同保留或共同淘汰，并记录 `evicted_turn_count`；**MUST NOT** 生成或复用摘要、rolling memory、replacement history。固定 System/当前用户消息/必要协议组自身超限时 Attempt 失败为 `CONTEXT_TOO_LARGE`。（来源：Issue #13 用户定案）
- **RUNTIME-AGENT-004 —** Chat streaming 的可见文本可经 `ModelTokenDelta` 转发 HTTP SSE，但 delta 是瞬时 UI 投影，**MUST NOT** 直接创建 Investigation Message 或成功输出；旧 epoch、未知/终态 Model Call、非单调 `delta_seq` 或 cancellation fence 后的 delta 必须丢弃。隐藏 reasoning/thinking token **MUST NOT** 通过该消息、日志或 Artifact 暴露。（来源：CONTEXT「模型调用边界」）
- **RUNTIME-AGENT-005 —** provider 返回或失败后 Plinth **MUST** 发送 `CompleteModelCall`。Quoin 在一个事务中封存 usage/latency/request ID/termination，并置物理调用终态；Chat 的规范 response 必须是 object 且总含 `tool_calls` 数组（无工具时为空），完整成功响应或任何已暴露的部分响应必须写 `model_call_outputs`（部分响应 `complete=0`）。成功响应中的全部 `ProposedToolCall` **MUST** 按 provider 顺序一次性验证并创建 pending `tool_calls`，随后在 `CompleteModelCallAck` 返回持久 ID、固定 `ToolFailureMode` 与必要的非秘密 grant 引用。任一 Tool schema/参数/路由不合法时，同一事务必须把物理 Model Call 与 Attempt 封存为 `invalid_response` 失败且不创建任何 Tool Call；**MUST NOT** 留下 running Model Call、先执行或部分创建 Tool Call。相同 model_call_id 的 `CompleteModelCall` 重放必须按已封存 output digest/outcome/tool_call IDs 幂等返回原 Ack；内容不一致必须冲突拒绝。wire `ModelCallFailureReason` 与 SQL `model_calls.termination_reason` 逐一映射为 `TIMEOUT->timeout`、`RATE_LIMITED->rate_limited`、`TRANSPORT_ERROR->transport_error`、`PROVIDER_UNAVAILABLE->provider_unavailable`、`CONTEXT_OVERFLOW->context_overflow`、`INVALID_RESPONSE->invalid_response`、`CANCELLED->cancelled`、`ARTIFACT_COMMIT_FAILED->artifact_commit_failed`；`ATTEMPT_TERMINAL` 只用于 cancellation fence/终态已提交后的拒绝 Ack，不创建新的物理行或改写既有终态，不得自造额外映射。（来源：DATA-TX-017）
- **RUNTIME-AGENT-006 —** provider 传输重试 **MUST** 是显式物理行：同一 `call_seq`、`retry_seq+1`，且前一物理行已明确 `failed`、没有 `model_call_outputs` 行，并且失败原因严格属于 `timeout|rate_limited|transport_error`；`transport_error` 只允许 adapter 能证明请求未到达 provider 或没有不确定副作用时使用。`provider_unavailable`、任何已暴露部分响应、取消 fence、Attempt 已终态、`invalid_response` 或 `artifact_commit_failed` 均不得在同一 Attempt 自动重试；`context_overflow` 只有在未收到任何响应且下一物理行增加 `evicted_turn_count` 时才可显式重试，固定必需项本身超限直接 `CONTEXT_TOO_LARGE`。provider SDK 内建隐式重试必须关闭。下一次 Agent 推理使用 `call_seq+1,retry_seq=0`，**MUST NOT** 与物理重试混淆。（来源：Issue #13、DATA-AUDIT-003）
- **RUNTIME-AGENT-007 —** Tool Calls **MUST** 严格按 `provider_index` 顺序执行。Plinth 在任何副作用前发送 `BeginToolCall`；Quoin 只在父 Model Call 已完整成功、Attempt Running、前序 Tool Call 已终态且 grant/Tool 定义闭合时将 pending 推进 running 并回 accepted。`ToolFailureMode` 来自发布的固定 Tool 定义：`RETURN_TO_MODEL` 把失败作为已提交 Tool Result 继续下一轮；`FAIL_ATTEMPT` 立即失败 Attempt。模型或 worker不得覆盖。（来源：Issue #13）
- **RUNTIME-AGENT-008 —** Tool 结束后 Plinth **MUST** 发送 `CompleteToolCall`。Quoin 在同一事务校验固定 result schema，提交 ToolCall 终态、确定性 Evidence、Artifact 引用与模型可见 `committed_payload` 后回 `CompleteToolCallAck`；`RETURN_TO_MODEL` 失败的 `committed_payload` 必须由 Quoin 根据 `error_code` 与经脱敏的 `error_detail` 确定性生成有界规范 JSON。Plinth 后续 Model Call 只能使用 Ack 返回的已提交结果。取消先提交、旧 epoch、正文已过期、未提交 Artifact 或引用不闭合时拒绝完成；取消前已提交的 Tool/Evidence/Artifact 历史保留。（来源：DATA-TX-018）
- **RUNTIME-AGENT-009 —** 模型输出只是提案：Quoin 只做类型/结构、引用闭合、digest、状态与 fence 校验，**MUST NOT** 以程序给模型结论设置 verdict/severity 下限或判断内容正确性。Agent 最终输出仍必须经 `ResultProposal` 原子落为固定工作模式的领域对象；部分 token、未闭合 Tool Call 或未提交引用不得成功。（来源：Issue #13 用户边界、DATA-ATTEMPT-010）
- **RUNTIME-AGENT-010 —** Embedding 与 Model Provider capability probe **MUST NOT** 创建 Agent worker 或 ReAct loop；Plinth supervisor 仍使用 `BeginModelCall`/`CompleteModelCall` 建立真实物理调用审计。Provider revision + credential generation 组合的 probe 必须真实执行 chat streaming/native tool/multi-tool/cancel/usage 与 embedding/dimension 测试，成功结果才可提交 `model_provider_capabilities` 并允许启用。（来源：Issue #13）

## 7. Plinth supervisor / worker 本地协议

- **RUNTIME-WORKER-001 —** `contracts/quoin/plinth/worker/v1/agent_worker.proto` 是 supervisor/worker 唯一机器协议，`architecture.md` 的 `ARCH-WORKER-*`/`ARCH-AGENT-*` 是其人类语义权威；本文件只规定该本地协议与 Quoin Runtime 协议的跨边界裁决，不复制 framing、完整消息状态机或 sandbox profile。（来源：Issue #13、ARCH-WORKER-001、ARCH-AGENT-001）
- **RUNTIME-WORKER-002 —** worker 的 Model/Tool/结果消息都只是提议。supervisor 必须先取得对应 Quoin `Begin*` Ack 才可发起 provider/Tool 副作用，并且只有在 Quoin 已提交完成事实后才可把完成结果返回 worker；`WORKER_LOCAL` 也必须经本地协议的 `ExecuteToolCall -> ToolCallStarted` 门后才能执行。（来源：Issue #13、ARCH-TOOL-001/002）
- **RUNTIME-WORKER-003 —** 收到 `CancelAttempt` 后，supervisor 必须按 `architecture.md` 的取消顺序执行；worker 的本地停止确认、EOF、stderr 或进程信号都不覆盖 Quoin cancellation fence，也不得自行裁决 Attempt 终态。（来源：Issue #13、ARCH-RECOVERY-001、DATA-TX-005）
- **RUNTIME-WORKER-004 —** worker 启动前必须通过 `architecture.md` 的 v1 同 uid Landlock + seccomp profile 真实自检；不能建立声明边界时 readiness/Attempt 以 `sandbox_unavailable` 失败且不得静默退化。隔离自检完成前不得发送 `StartAttemptAck`，Quoin 不得把该 worker 视为可执行。（来源：Issue #13 Q13.4 A、ARCH-WORKER-007/008）

## 8. 跨 Runtime 子执行（Plinth → Quoin → Lintel）

- **RUNTIME-CROSS-001 —** Plinth 的浏览器工具请求 **MUST** 经 `RequestBrowserSubExecution`（控制流，Plinth -> Quoin）提交：携带 `parent_attempt_id`、已持久化且全局唯一的 `tool_call_id` 与 `BrowserSubExecutionInput`（`schema_kind="browser_tool_v1"`）。`canonical_json` 必须逐字等于既有 `tool_calls.arguments_json` 的 canonical bytes，digest 必须同时匹配既有 `arguments_digest` 与 SHA-256，并通过 [browser-tool.schema.json](contracts/schemas/browser-tool.schema.json) 的 request 子 Schema；子执行幂等键就是既有 `tool_call_id`。Plinth 与 Lintel **MUST NOT** 直接通信。（来源：Issue #13、Issue #14、ARCH-TOOL-002、CONTEXT「Plinth」）
- **RUNTIME-CROSS-002 —** `open` action 只携带人类领域键 `businessSystemKey`，Quoin 解析该系统唯一 Browser Identity；后续 action 只携带本父 Investigation 当前活动的 opaque `sessionId`，不得携带数据库 identity locator。Quoin **MUST** 在同一事务复核父 Attempt 为 `Running investigation`、Tool Call 为同 Attempt 的 `running quoin_browser`、参数 digest 匹配，并为每个有效请求创建唯一 `browser_exploration` 子 Attempt。首个 `open` 在身份空闲时创建会话级 Exploration Browser Operation；同身份已占用仍创建并闭合本次子 Attempt，以正常结构化 `IdentityBusy` Tool Result 返回，不建立身份等待队列。后续 action 必须映射到同父 Attempt、同 operation；相同 `tool_call_id` 重放返回原 child Attempt，不重复创建。（来源：Issue #13、Issue #14、DATA-ATTEMPT-007、DATA-BROWSER-007）
- **RUNTIME-CROSS-003 —** 子 Attempt 派发 Lintel（RUNTIME-TASK-003）；全局浏览器容量不足时首个 `open` 的 operation 与子 Attempt进入 Quoin FIFO，后续 action 复用既有 Session/slot。父 Failed/Cancelled/Interrupted fence 先提交时，同一事务 fence 所有子 Attempt、关闭 active Exploration Session 并使对应 Tool Call cancelled；此后的子结果只能留审计，不能产生有效 Tool Result/Evidence 或复活父 Attempt。子结果先完整提交则保留历史，父随后仍按自身 fence 收口。（来源：Issue #13、Issue #14、DATA-TX-005、ARCH-RECOVERY-001）
- **RUNTIME-CROSS-004 —** 每个子 Attempt 的 `ResultProposal` payload **MUST** 为 `browser_exploration_result_v1` 并通过 [browser-tool.schema.json](contracts/schemas/browser-tool.schema.json) 的 result 子 Schema。Quoin 在同一事务复核父仍 `Running`、Tool Call 仍 `running`、`browser_exploration_actions` 绑定同 operation/Tool Call/child Attempt，再更新动作审计与 Tool Call，随后向 Plinth 发送 `ToolResultDelivery`。`ElementNotFound|ElementNotUnique|ActionTimeout|NavigationFailed|DialogBlocked|DownloadBlocked|ElementReferenceStale|IdentityBusy` 等可恢复结果完成当前子 Attempt但保持 Session；明确未登录、profile/Chromium 崩溃、Runtime/协议失效、trace commit 失败、取消、父终态或 lease 丢失才关闭 Session。Ack 丢失时按持久 Tool Result 幂等重发；**MUST NOT** 建立通用任务 DAG。（来源：Issue #13、Issue #14、ARCH-TOOL-002/004）

## 9. 浏览器控制、执行与 noVNC 隧道

- **RUNTIME-BROWSER-001 —** `Hello` **MUST** 由 Lintel 声明精确 Chromium revision 与固定浏览器总 slot 数；同一 boot 内不得改变上限。Quoin 是唯一容量队列：manual login、authentication probe、Journey 与整个 Exploration Session 都必须先创建 `browser_operations` 行并各占一个 slot，容量不足只按该表 `id` 的 SQLite 提交创建序进入全局 FIFO `WaitingForCapacity`；Inspection Run/Config Test Run 保持 `Running`，等待提示直接由 operation 投影。不设配额、预留或抢占；`Heartbeat.capacity` 只报告当前占用/上限，不创建第二队列。Lintel 若以 `NO_CAPACITY` 拒绝 Start 只能表示容量投影已陈旧，必须保证未创建进程；operation 回到 `WaitingForCapacity`，但保留原 FIFO 身份与已冻结的 Start 派发 boot/epoch fence；Quoin 等下一次容量投影后在同 boot 以相同 operation ID 幂等重发，Lintel 对尚未创建进程的 `NO_CAPACITY` 不得缓存成永久拒绝，也不得自行排队。（来源：Issue #14 Q14.5）
- **RUNTIME-BROWSER-002 —** 每次 Lintel 新 boot 通过 `HelloAck` 后、接收任何 Browser Operation 前，Quoin **MUST** 发送带同 boot 唯一且重发稳定 `inventory_id` 的全量 `ProfileInventoryRequest`，列出每个 current generation 的 identity、generation、manifest digest 与精确 Chromium revision；Lintel **MUST** 回显该 ID 的单份完整 `ProfileInventoryReport`，只检查目录、原子 manifest 与 revision，不启动 Chromium；协议不提供部分报告，旧 ID 报告只作审计，不得解除 readiness fence。缺失、manifest 无效或 revision 不匹配由 Quoin 依 DATA-BROWSER-009 降级；报告外孤儿 profile 不得成为 generation。inventory 完成前 Lintel browser capacity 不 Ready。（来源：Issue #14 Q14.23）
- **RUNTIME-BROWSER-003 —** 无 Attempt 的 `manual_login|authentication_probe` **MUST** 使用控制流 `StartBrowserOperation`/`StartBrowserOperationAck`；除成功人工发布由 RUNTIME-BROWSER-004 的 `PublishBrowserProfileResult` 作为单一终局提交入口外，其余 Lintel 主动完成使用 `CompleteBrowserOperation`/`CompleteBrowserOperationAck`；Quoin 先持久化 operation 和冻结 input，到达 FIFO 队首时在同一事务写 `Starting`、`start_dispatched_at` 与当前 Lintel boot/epoch 后才发送 Start。manual login 启动独立 Chromium+x0vncserver；probe 运行当前 revision 引用的版本化 Journey。Ack/Result 必须携带 operation ID，并来自 SQL 冻结的同一 Lintel boot；首次派发 epoch 是不可变审计绑定，同 boot 重连后可在完成调和的当前更高 epoch 流上幂等返回同一 Ack/Result，不得要求已断开的旧 epoch 继续存在；`accepted=false` 必须保证未创建进程且 `reject_reason` 为 `BrowserOperationStartRejectReason` 的非 `UNSPECIFIED` 值：`NO_CAPACITY` 按 RUNTIME-BROWSER-001 回到 `WaitingForCapacity` 重发，其余拒绝必须在同一事务持久化封闭 Start reject reason/时间、`Failed` 终态与 `stop_confirmation_basis=start_rejected`；operation terminal reason 的映射固定为：`authentication_required` 同名，`profile_unavailable` 依据已经持久化的 inventory 事实精确化为 `profile_missing|profile_manifest_invalid|chromium_revision_mismatch`，`identity_busy|input_unsupported|reconcile_required|stale_stream|internal` 统一映射 `protocol_error`，不得由实现自行选择。`CompleteBrowserOperation` 必须携带规范 payload 的 32-byte SHA-256 `result_digest`；Quoin 只在 SQL 单事务完成终态持久化后回 `CompleteBrowserOperationAck{accepted=true}`，并把 digest 写入 operation。Ack 丢失时 Lintel 在同 boot 调和后的当前流重发同一 operation+digest，Quoin 对同 digest 返回相同接受裁决；不同 digest、未知/不匹配 operation 或已被别的终局收口返回 `accepted=false`，不得覆盖既有终局。Lintel 在收到 accepted Ack 前不得把“已发送”视为“已提交”，但可保留受保留策略约束的临时重发材料；所有 Artifact 仍须先完成 ArtifactService 提交。Lintel 不得分配领域 locator 或自行改变身份状态。（来源：Issue #14 Q14.1/Q14.2/Q14.6）
- **RUNTIME-BROWSER-004 —** 人工发布 **MUST** 使用 `PublishBrowserProfile`/`PublishBrowserProfileResult`：请求绑定 active manual-login operation、预定 generation 与幂等 command ID；Lintel 在同一 Chromium 上执行 publish probe，只有 `Authenticated` 才原子写 profile manifest 并返回精确 Chromium revision 与 manifest digest。相同 command/operation/generation 重试返回同一结果；Quoin 收到成功后才依 DATA-BROWSER-002 单事务创建 generation、切指针并结束 operation。`Unauthenticated` 保持登录 operation 可继续，`Indeterminate` 返回技术失败且不得改变身份事实。（来源：Issue #14 Q14.1/Q14.7/Q14.12）
- **RUNTIME-BROWSER-005 —** Journey 与 Exploration **MUST** 在 operation 到达 FIFO 队首后先把 operation 置 `Starting` 并经 `StartBrowserOperation` 预留物理 slot；Start 按 operation ID 幂等，Ack 丢失时 `Starting` 持续保留身份/容量 fence，同 boot 重连完成调和后在当前更高 epoch 重发 Start 并取得同一结果；只有未在 lease 内同 boot 恢复或出现新 boot 才先收口为 `Interrupted` 并等待停止确认。只有 Ack accepted 并把 operation 置 `Running` 后才向 Lintel 派发对应 Journey/首个 Exploration 子 Attempt；`NO_CAPACITY` 按 RUNTIME-BROWSER-001 原 ID 重发，其他拒绝或派发失败必须终止 operation 并走停止 fence。两者每 operation 启动独立 Chromium，独占打开冻结 generation；Lintel 禁止进程池、长期身份进程或跨 operation 复用 tab/context。当前 Chromium revision 与 generation 不完全相等时不得打开 profile。Journey 不自动整单重跑或中点恢复；Playwright locator 的固定 deadline 等待不算重试。（来源：Issue #14 Q14.4/Q14.10/Q14.20）
- **RUNTIME-BROWSER-006 —** authentication probe 的机器结果封闭为 `AUTHENTICATED|UNAUTHENTICATED|INDETERMINATE`；技术错误不得伪装未登录。Journey 第一执行步骤前和成功提交前、Exploration Session 建立和成功关闭前各执行一次；运行中匹配版本化明确未登录条件可提前终止。每次结果连同 phase、Journey ID、catalog digest/version 持久化；`UNAUTHENTICATED` 才降级身份。（来源：Issue #14 Q14.2/Q14.7/Q14.17）
- **RUNTIME-BROWSER-007 —** `browser_exploration_v1` 动作与 `browser_exploration_result_v1` 观察的唯一机器形状是 [browser-tool.schema.json](contracts/schemas/browser-tool.schema.json)：动作封闭集合只由该 Schema 定义；禁止任意 JavaScript、Playwright、CDP、raw HTTP、上传与下载，意外下载必须阻止并返回 `DownloadBlocked`。Lintel 不维护 origin allowlist，但必须记录顶层导航、重定向链和新窗口 origin。（来源：Issue #14 Q14.8/Q14.9/Q14.14）
- **RUNTIME-BROWSER-008 —** 观察结果 **MUST** 有界返回 URL/origin/title、page 列表、可访问性/可见文本投影、短期 element reference、交互状态和 navigation/redirect/popup/dialog 事件，并携带 observation version、原始大小与 `truncated`；不得返回完整 HTML/DOM、storage state、Cookie、网络正文或浏览器内部对象。element reference 只在同 Session 且匹配 observation version 时有效。（来源：Issue #14 Q14.15）
- **RUNTIME-BROWSER-009 —** 每个 Exploration Session **MUST** 按 operation ID 在 Lintel 临时 staging 中产生一份连续敏感 trace；逐动作输入/结果不得携带尚未完成的 Artifact locator。Session 结束时只上传一次，全部子 Attempt 经持久 action → operation 关系派生引用同一 Artifact。正常成功关闭前 completion probe 必须 `Authenticated`、trace 必须 complete 且已提交、operation 必须已原子置 `Succeeded`，此后才能把 `close_session` 子 Attempt 与 Tool Result 置成功并交还模型；取消/崩溃时尽力提交并标 `incomplete`。Journey 失败 trace 同样是强制诊断 Artifact；上传失败以 `ARTIFACT_COMMIT_FAILED` 结束，不得把操作或 check 判为成功，并必须在 ResultProposal/ledger 的 `originalGapCode=journey_failed` 保留被覆盖的原失败分类，`errorDetail` 同时说明原 step 失败与 trace 提交失败。（来源：Issue #14 Q14.11/Q14.22）
- **RUNTIME-BROWSER-010 —** 每个活动 manual-login BrowserSession **MUST** 使用一条独立 `BrowserTunnel.Open` bidi stream；持久审计权威是 `browser_operations`，session ID、attachment 与宽限期是内存投影。流首帧 `BrowserSessionOpen` 必须绑定 operation/identity/actor user/slot/boot/epoch；Quoin 只接受该用户当前 active manual-login operation。（来源：Issue #14 Q14.1/Q14.19）
- **RUNTIME-BROWSER-011 —** 同一 manual-login operation 任何时刻最多一个 noVNC attachment；已有 attachment 时第二连接冲突。前端 WebSocket 断开后 operation 进入 `AwaitingReconnect`，只有同一用户可在同 boot 宽限期内重附着；超期 `GRACE_EXPIRED`。BrowserSession、Chromium 与 operation 不跨 Quoin/Lintel 进程重启恢复。（来源：Issue #14 Q14.1/Q14.19）
- **RUNTIME-BROWSER-012 —** noVNC/RFB 字节只在 `BrowserFrameData.payload` 双向透明中继；Quoin **MUST NOT** 解析、校验、记录或改写内容。Session 吊销/账号禁用/登出立即 `SESSION_REVOKED`；slot 吊销/替换、新 boot 与 shutdown 立即关闭隧道并触发 operation `Cancelled|Interrupted` 收口；只要原 Lintel 仍可通信，就必须先按 RUNTIME-BROWSER-013 尽力提交 trace/completion，再提交领域终态并等待物理停止确认。新 boot 或 Runtime 失联已经证明旧 Session 不可恢复、同时也可能使 trace 永久不可取得；此时必须以 `Interrupted` + `new_boot|runtime_unavailable` 明确记录 trace 缺失，绝不得伪装完整或成功。（来源：HTTP-NOVNC-001、Issue #14 Q14.1/Q14.11/Q14.22）
- **RUNTIME-BROWSER-013 —** 显式取消父 Attempt 时，Quoin **MUST** 先提交 `Cancelling` fence，再请求 Lintel 收口 operation，operation 终态后父 Attempt 才能进入 `Cancelled`；父 Attempt 自然准备 `Succeeded|Failed|Interrupted` 时则保持 `Running`，先收口 operation，再直接提交该目标终态，`Cancelling` 不得被伪装成通用 close 状态。Lintel 必须先尽力提交强制 trace 与适用的 completion probe，数据库不得用父状态更新直接伪造浏览器终局。成功发布或任何 operation 收口事务提交后，Quoin **MUST** 发送幂等 `StopBrowserOperation` 作为 Lintel 进程与隧道停止 fence；该消息只传播已提交事实，不授权 Lintel 改写领域终态；Lintel 只能停止该 operation ID，不得按 identity 杀死后续 operation，重复发送及目标已关闭均回 `StopBrowserOperationAck{stopped=true}`。Start 已派发的 operation 在领域终态提交后 `stop_confirmed_at` 仍为空，身份锁与物理 slot 不释放；Start 尚未派发者以 `not_dispatched`、已持久化非容量拒绝者以 `start_rejected` 在终态事务内直接确认停止；收到当前 boot 的活动流 Stop Ack 后以 `stop_ack` 一次性写入；同 boot inventory 明确报告不存在时以 `inventory_absent` 写入；接受新 Lintel boot 时以 `new_boot` 批量确认。`new_boot` 只证明旧进程消失，不得改写已经提交的领域 terminal reason。`stop_confirmed_at` 与该封闭 basis 必须成对且不可改写；Ack 丢失不得冒充拒绝。同 boot 重连的 `Hello`/`Heartbeat.active_browser_operations` 必须调和实际集合并对仍存活终态 operation 重发 Stop。成功发布后 noVNC 与 Chromium 必须立即关闭；未发布关闭不得恢复此前 `Ready`。相同 operation 的后续 tunnel/open/result 帧只作迟到审计并拒绝产生新 generation。（来源：Issue #14 Q14.1/Q14.3/Q14.11/Q14.12/Q14.19/Q14.22）
- **RUNTIME-BROWSER-014 —** 每个 Browser Tool Call **MUST** 创建一个 `browser_exploration` 子 Attempt。首个 `open` 的准入事务先裁决身份：命中 `IdentityBusy`、身份尚需登录或当前 profile 不可用时不创建虚假 operation/action，而是创建 operationId=null 的不可变输入快照并以 `actionSeq=0` Tool Result 本地收口（错误分别为 `IdentityBusy|AuthenticationRequired|ProfileUnavailable`）；准入成功时同一事务先创建 operation，再创建引用该 operation/identity/profile/catalog 的子 Attempt 与不可变 `browser_exploration_v1` 输入。后续动作引用既有 operation。只有需要 Lintel 执行的子 Attempt 才受 operation ledger 的全局容量 FIFO 裁决；operation 到达队首并预留 slot 后才派发，子 Attempt 被 Lintel 接受为 `Running` 后、执行动作前创建 `browser_exploration_actions` 行；一次子 Attempt 只执行一个 `browser-tool.schema.json` action，页面/session 状态由会话级 operation 跨调用保留。可恢复与终止错误码的封闭分区只由 `browser-tool.schema.json` 定义并由 SQL/Schema 漂移检查逐项验证；可恢复动作失败作为 `browser_exploration_result_v1` 正常返回模型且不结束 Session，身份未登录、profile/Chromium/Runtime/协议/trace、取消、父终态或 lease 失效才关闭 Session，模型不得覆盖分类；运行中 `ProfileUnavailable` 必须按观测到的具体事实映射为 `profile_missing|profile_manifest_invalid|chromium_revision_mismatch`，不得猜测为 `browser_crashed`。（来源：Issue #14 Q14.3/Q14.24）
- **RUNTIME-BROWSER-015 —** `ProfileInventoryReport.complete` **MUST** 为 true，且报告必须与请求 identity 集合完全相等且无重复；Quoin 逐项按当前 generation 精确校验 manifest digest 与 Chromium revision，并把结果写 `browser_profile_reconciliations`：目录不存在为 `missing`；manifest 不可读/无效/摘要不符为 `manifest_invalid`；仅当 manifest 摘要精确相等而 Chromium revision 不等时才为 `chromium_revision_mismatch`。`missing|manifest_invalid|chromium_revision_mismatch` 立即置身份 `AuthenticationRequired`；`compatible` 只证明文件/版本兼容，不运行 probe、不证明仍登录。Quoin **MUST** 先验证整份报告，再在单个事务写入该 inventory 的全部 reconciliation 与身份降级；验证失败或事务未提交不得保留部分行，也不得启动/恢复任何 Browser Operation。（来源：Issue #14 Q14.23）
- **RUNTIME-BROWSER-016 —** `browser_exploration_actions` 的长期结构化日志只保存 action kind、非秘密目标描述、page/origin、时间、结果/error code、观察摘要 digest/大小与 Artifact 引用；`fill`/`press` 实际值和页面正文不得复制入日志。模型看到的有界页面观察只存在于对应 Tool Result/Evidence，避免第二份正文权威。（来源：Issue #14 Q14.18）

## 10. Artifact 上传与按需读取（`ArtifactService.Upload`）

- **RUNTIME-UPLOAD-001 —** 每个上传 **MUST** 使用独立 client-stream：首帧 **MUST** 为 `ArtifactUploadHeader`（upload_id、attempt_id、boot_id、connection_epoch、owner、kind、retention_kind、sensitive、size_bytes、sha256、media_type；Journey/Exploration 连续 trace 的 attempt_id=0，由 operation 自身授权），随后 0..N 个 `ArtifactUploadChunk`（offset 连续），可显式 `ArtifactUploadEnd`；Quoin 在收到 `size_bytes` 字节或 `end` 帧后裁决。（来源：Issue #11、DATA-ARTIFACT-006）
- **RUNTIME-UPLOAD-002 —** v1 **MUST** 整单重传，**MUST NOT** 实现 offset 续传；`upload_id` 在重试生命周期内稳定：同 upload_id 同摘要重试 **MUST** 返回原 `artifact_id`，同 upload_id 不同摘要/owner **MUST** 冲突拒绝（DATA-ARTIFACT-006）。
- **RUNTIME-UPLOAD-003 —** Quoin 提交 **MUST** 依 DATA-ARTIFACT-001 的耐久顺序：临时文件写入并校验 hash/大小 → fsync 临时文件 → 按 SHA-256 原子 rename → fsync 最终父目录 → 才提交 SQLite 引用事务（`artifact_blobs` + `artifacts` + ledger `uploading->committed`）；引用事务失败 **MUST** 将文件作为孤儿清理。
- **RUNTIME-UPLOAD-004 —** `committed` 裁决 **MUST** 满足全部正向条件（DATA-ARTIFACT-006 与 SQL 触发器）：普通上传要求 Attempt `Running` 且 `boot_id`/`connection_epoch` 非空并与 upload 一致；Journey/Exploration 连续 trace 改由仍为 `Running` 的 browser operation 及其首次派发时冻结的 Lintel boot/epoch 精确授权（同 boot 重连后的上传 header 仍复制该 owner 冻结绑定，而不是当前上传流 epoch），因此父 Attempt 进入 `Cancelling` 或两个 Tool Call 之间没有活动 child Attempt 时仍可提交，且不得伪造一个 Tool Call Attempt；JOIN 的 `artifacts`+`artifact_blobs` 与 ledger 的 kind/retention_kind/sensitive/owner_type/owner_id/sha256/size_bytes 精确匹配；任一条件不满足 **MUST** 置 `rejected` 并返回 `UploadRejectReason`（旧 Attempt 终态、替换后 ABA、boot/epoch 不符、元数据不符均不得 commit）。
- **RUNTIME-UPLOAD-005 —** `trace` 上传 **MUST** `sensitive=true`（DATA-ARTIFACT-003）；ledger 来源字段与 `artifact_id`/`committed_at` 提交后 **MUST NOT** 改写；历史 **MUST NOT** 删除。
- **RUNTIME-UPLOAD-006 —** chunk 大小与流背压数值为部署配置（RUNTIME-SCOPE-004）；队列溢出 **MUST** 可观察且不静默丢弃。
- **RUNTIME-ARTIFACT-001 —** Tool 结果字节数 `> 50 * 1024` 或行数 `> 2000`（任一先满足）时 Plinth **MUST** 以有界流提交现有 `kind=tool_result,retention_kind=generated` Artifact；模型只收到可判别预览、Artifact locator、media type、大小、行数与 hash，并被告知可调用固定 Artifact Read/Grep Tool。文件/搜索结果预览优先头部，日志/命令输出优先尾部；上传失败不得把截断预览伪装成完整成功。（来源：Issue #13 用户定案；Pi/OpenCode 上游研究）
- **RUNTIME-ARTIFACT-002 —** `ReadText`/`GrepText` 是 supervisor-only、Attempt-scoped RPC：Quoin 必须校验 Runtime Bearer、slot=plinth、Attempt Running、boot/epoch 当前且 `attempt_artifact_grants` 已授权；Artifact 必须是允许的文本 media type 且 `body_expired=0`。worker 只经本地固定 Tool 请求，永不获得 Runtime Bearer、storage_key 或 Quoin PV 路径。（来源：Issue #13 用户定案、ARCH-INPUT-004、DATA-ARTIFACT-003）
- **RUNTIME-ARTIFACT-003 —** Read/Grep 的行数、匹配数、上下文行和响应字节数使用服务端固定上限；`start_line` 1-based，Grep 使用 RE2 语义以避免不受控回溯。响应必须回传完整 Artifact 的 size/hash/media type 与 eof/truncated，使模型能判断是否继续读取，**MUST NOT** 暗示已返回完整正文。（来源：Issue #13 用户定案、ARCH-INPUT-005、DATA-ARTIFACT-007）
- **RUNTIME-ARTIFACT-004 —** generated Artifact 到期由现有 GC 只清理正文；Artifact owner/hash/media type/大小/来源历史继续保留。正文已过期时 Read/Grep 返回 `FAILED_PRECONDITION`（detail=`ARTIFACT_BODY_EXPIRED`），若它是当前 Attempt 不可替代的必要输入则 Attempt 失败为同名结构化原因，**MUST NOT** 从非权威本地缓存继续。（来源：Issue #13 用户定案、ARCH-OUTPUT-004、DATA-ARTIFACT-003/004）

## 11. 注册与轮换

- **RUNTIME-REG-001 —** 一次性注册令牌只存在于进程内存，固定 60 秒、单次成功消费、绑定发起 Admin Session、slot 与 generation；HTTP `prepareRuntimeRegistration` + `revealRuntimeRegistrationToken` 遵循 SEC-REVEAL-*。令牌/handle 不得进入 SQLite、日志、审计、Artifact 或命令持久结果。（来源：Issue #16、HTTP-COMMAND-012、RUNTIME-AUTH-006）
- **RUNTIME-REG-002 —** `RuntimeControl.Register` **MUST** 只接受注册窗口中的请求：验证内存令牌的 slot/generation、单次消费和 release version；同一事务创建已确认 credential、设置 current、slot 转 registered，返回长期 token/generation。supervisor 原子持久化到 0600 状态卷。响应丢失后令牌不得重放，恢复必须由 Admin 再次 replace。（来源：DATA-RUNTIME-001/002、SEC-REVEAL-*）
- **RUNTIME-REG-003 —** slot 已 registered、generation/slot 不匹配、token 过期/消费等拒绝映射保持封闭 canonical gRPC status；detail 只含非秘密说明。（来源：RUNTIME-ERROR-001、SEC-ERROR-001）
- **RUNTIME-REG-004 —** 长期 token 两阶段轮换只走已认证控制流：① Quoin 创建未确认 credential 并设 pending，发送 `IssueToken`；② Runtime 原子持久化新 token 后回 `TokenPersisted`；③ Quoin写 `confirmed_at`，单条 UPDATE 把 pending 提升为 current、原 current 移入 retiring；④ Quoin 以 `GoAway{ROTATED}` 关闭旧 epoch，Runtime 用新 token 重连；⑤ 新 current 首次成功认证时写 `first_authenticated_at`，旧 generation 进入用户可见 Pending Retirement；⑥ 只有 Admin 的 `retireRuntimeCredential` 才清 retiring 并永久退休旧 generation。步骤⑤ **MUST NOT** 自动执行步骤⑥，也不设 TTL。（来源：CONTEXT「服务身份」「安全、身份与恢复」、Issue #16、DATA-RUNTIME-002）
- **RUNTIME-REG-005 —** 轮换期间认证只接受 registered slot 的 current 或 retiring 指针；pending 永不接受。若 retiring token 重连成功，它获得新的唯一 connection epoch 并使旧 epoch 失效，但不得把该 generation 自动恢复为 current。Admin 退休后对应全部流立即关闭且禁止重连。（来源：Issue #16、SEC-SERVICE-003、RUNTIME-CTRL-003）
- **RUNTIME-REG-006 —** 轮换中止必须先清 pending（SQL 自动退休未提升 generation），随后继续 current 连接；已退休凭据不得确认、复活或重新设为任一角色指针。（来源：DATA-RUNTIME-002）
- **RUNTIME-REG-007 —** `Register` 失败 **MUST** 使用 canonical gRPC status（RUNTIME-ERROR-001）：`INVALID_ARGUMENT`（请求缺字段/generation=0/token 文本非 base64url 或解码非 32 字节）、`UNAUTHENTICATED`（令牌未知/过期/已消费）、`FAILED_PRECONDITION`（slot 不在注册窗口——含 `ALREADY_REGISTERED`——或 generation 与令牌记录不一致）、`PERMISSION_DENIED`（令牌绑定 slot 与请求 slot 不一致）、`NOT_FOUND`（未知 slot，不应发生）、`INTERNAL`（瞬态内部错误）。detail 只含非秘密说明。

## 12. 吊销与关闭

- **RUNTIME-REVOKE-001 —** 凭据吊销、slot 替换或账号级吊销 **MUST** 立即关闭对应 Runtime 的全部 Control/Browser/Upload 流并拒绝重连（Q214 冻结、架构记忆 #6034）；关闭是权威，`GoAway`/`BrowserSessionClose` 是尽力而为通知。
- **RUNTIME-REVOKE-002 —** 替换 fence **MUST** 与 DATA-RUNTIME-001b 一致：`plinth` 置 `revoked` 前无绑定该 slot 的 active Attempt（`Assigned`/`Running`/`Cancelling`）；`lintel` 置 `revoked` 前无状态为 `Queued|WaitingForCapacity|Starting|Running|AwaitingReconnect` 或虽已终态但 `stop_confirmed_at` 为空的 Browser Operation；BrowserSession 瞬时锁在 Quoin 内存临界区与替换事务同 fence。
- **RUNTIME-REVOKE-003 —** 替换与派发 **MUST** 按 SQLite 提交顺序裁决：替换先提交则后续派发被拒（DATA-RUNTIME-001 触发器兜底）；派发先提交则替换被拒；revoked 期间旧凭据一律拒绝。

## 13. 连接凭据 grant（`RuntimeControl.FetchCredentialGrant`）

- **RUNTIME-GRANT-001 —** 连接凭据必须由 Quoin 以不可变 `attempt_connection_grants` 授权：模型/Embedding grant 可在派发前随 `AttemptInputSnapshot` 下发；Thanos/Kubernetes grant 只能在 Quoin 接受具体 Tool Call 并根据全局 Thanos 或当前 Business System mappings 确定性路由时创建，再由 `CompleteModelCallAck` 返回非秘密 `ConnectionGrant`。grant 引用携带 `grant_id`/revision/generation/purpose；秘密正文 **MUST NOT** 进入 worker、Dispatch、模型上下文、日志或审计。（来源：DATA-CONN-002/006/008、Issue #13）
- **RUNTIME-GRANT-002 —** supervisor **MUST** 经 `FetchCredentialGrant` 获取秘密：Quoin 校验 grant/Attempt/boot/epoch/slot，并重验 Connection current generation 的 root binding revision；随后在受控内存中以 SEC-KEY-003 的 AAD 解密。成功只返回类型化非秘密 config 与 secret 给 supervisor，worker 不得调用。binding 不匹配或 AEAD 认证失败必须拒绝 grant、隔离 Connection 并写无秘密审计，绝不尝试旧 key/旧 generation/空凭据。（来源：DATA-CONN-002/009、Issue #16、SEC-KEY-005）
- **RUNTIME-GRANT-003 —** grant 生命周期 **MUST** 只存在于 Attempt 运行期间：派发或具体 Tool 授权时创建、Attempt 达到终态（Succeeded/Failed/Cancelled/Interrupted）时 Quoin **MUST** 撤销获取资格并释放内存秘密，持久 grant 行继续作为不可变审计历史；Attempt 未 `Running` 或 grant 已撤销时 `FetchCredentialGrant` **MUST** 返回 `FAILED_PRECONDITION`（或 `NOT_FOUND`，detail 说明）；supervisor 侧 **MUST** 在 Attempt 终态后丢弃内存秘密，**MUST NOT** 落盘、进入日志/审计或响应快照。成功响应经 TLS 每次可重取（Attempt Running 期间幂等），但 supervisor **MUST NOT** 持久化。（来源：Issue #13、DATA-CONN-008）

## 14. Stele 告警接入（`SteleRelay`）

- **RUNTIME-STELE-001 —** Stele **MUST NOT** 注册为 Runtime（无 slot、不持有 Runtime 凭据）；其 service token 由部署 Secret 文件提供（CONTEXT「服务身份」），经 metadata 认证 `SteleRelay` 两个 unary RPC。
- **RUNTIME-STELE-002 —** Stele **MUST** 经 `GetCredentialSnapshot` 获取版本化只读凭据 digest 快照并仅内存缓存；快照只包含 SQL 中 `Active|PendingRetirement` 的可认证 generation，Retired 必须消失。未成功加载快照拒绝接收 Delivery；快照版本单调递增，Stele 提交时回传（DATA-ALERT-008）。
- **RUNTIME-STELE-003 —** `Deliver` **MUST** 携带 `relay_id`（每次外部请求生成，内部重试复用）、`source_id`、`credential_id`、`credential_snapshot_version`（>= 1）、`protocol`、精确原始 `body` 与 `received_at`；Quoin **MUST** 在 Delivery 事务内重验来源启用、凭据有效性与归属，relay_id 幂等（DATA-ALERT-001/008）。
- **RUNTIME-STELE-004 —** Delivery 使用新 generation 首次成功提交时，同一 SQLite 事务写该 generation `first_used_at` 并把其 supersedes 指向的旧 generation转为 PendingRetirement；Stele 后续快照继续包含新旧两个 generation，直到 Admin 显式退休旧值。系统不得以墙钟或首次成功自动移除旧 digest。（来源：Issue #16、SEC-SERVICE-002、DATA-ALERT-009）
- **RUNTIME-STELE-005 —** `DeliveryRelayResponse` 状态 **MUST** 映射 HTTP 语义：`ACCEPTED`=204（单事务保存 Delivery、处理结果并更新全部正常 Occurrence 后才返回）；`REJECTED`=4xx（永久拒绝，Alertmanager 不重试）；`UNAVAILABLE`=5xx（可重试）。提交失败或结果不确定 **MUST NOT** 返回 ACCEPTED（CONTEXT「Stele」）。
- **RUNTIME-STELE-006 —** 每个 unary 请求 **MUST** 携带 `release_version`（机器契约字段），与 Quoin 严格相等（RUNTIME-VERSION-001）；不匹配 **MUST** 返回 `FAILED_PRECONDITION` 且 **MUST NOT** 处理请求（Stele 不 Ready、拒绝接收 Delivery）；`GetCredentialSnapshotResponse.quoin_release_version` 供 Stele 快速核对，不一致时 Stele **MUST** 视为版本不匹配。

## 15. 版本与兼容

- **RUNTIME-VERSION-001 —** 四组件 **MUST** 以同一 Release 部署：`release_version` 严格相等（Connect 握手、Register、Stele unary 全部适用），**MUST NOT** 支持协商、前缀匹配或 N/N-1 混跑（CONTEXT「协调升级」）。
- **RUNTIME-VERSION-002 —** proto 字段编号一经分配 **MUST** 永不复用；删除字段 **MUST** 使用 `reserved` 声明并保留编号；oneof 成员与普通字段共享编号空间（SPEC-VERSION-003）。
- **RUNTIME-VERSION-003 —** 本文件所有新增字段（envelope 四字段、AttemptInputSnapshot/ResultPayload、Agent Model/Tool 消息、Artifact Read/Grep、子执行消息、grant 消息、release_version 字段）**MUST** 在 proto 与 descriptor 中可机器校验；任何一方实现缺失即视为版本不匹配，**MUST NOT** 静默降级。（来源：Issue #13、SPEC-VERSION-001）

## 16. 错误与边界验证

- **RUNTIME-ERROR-001 —** unary RPC（`Register`/`FetchCredentialGrant`/`ReadText`/`GrepText`/`GetCredentialSnapshot`/`Deliver`）失败 **MUST** 使用 canonical gRPC status：`INVALID_ARGUMENT`（缺字段/越界/格式错误）、`UNAUTHENTICATED`（令牌缺失或无效）、`PERMISSION_DENIED`（身份有效但无权，如 lintel 请求 grant）、`NOT_FOUND`（未知资源）、`FAILED_PRECONDITION`（状态前提不满足，如注册窗口外、Attempt 非 Running、版本不匹配）、`INTERNAL`（瞬态内部错误）。流内拒绝使用封闭枚举（`HelloRejectReason`/`AttemptRejectReason`/`UploadRejectReason`/`BrowserCloseReason`/`BrowserSubExecutionRejectReason`）；`*_UNSPECIFIED=0` 在请求/拒绝响应需要原因时一律无效（RUNTIME-VALIDATION-004）。detail **MUST NOT** 含秘密。（来源：Issue #13、RUNTIME-AUTH-004）
- **RUNTIME-VALIDATION-001 —** `contracts/runtime.proto` **MUST** 通过 proto 编译与 lint（buf STANDARD；本规格显式声明的例外：`SERVICE_SUFFIX`/`RPC_REQUEST_STANDARD_NAME`/`RPC_RESPONSE_STANDARD_NAME`（bidi 流用 envelope 而非 Request/Response 命名）、`RPC_REQUEST_RESPONSE_UNIQUE`（`Connect`/`Open` 双向同一信封类型）、`PACKAGE_DIRECTORY_MATCH`（既有 `runtime.proto` 固定在 `contracts/` 根目录；worker 独立 package 已按 package path 位于 `contracts/quoin/plinth/worker/v1/`，不使用该例外）），并生成 descriptor 检查：service/method 数量与 streaming 方向、字段编号唯一且不复用、oneof/reserved 合法、枚举值与 `schema.sql` 对应 CHECK 枚举一致（含 `ScopeType.SCOPE_TYPE_CONFIG_TEST_RUN` 与 `execution_attempts.scope_type` 的 `config_test_run`）、`Hello.journey_catalog_digest` 与 `HelloRejectReason.HELLO_REJECT_REASON_CATALOG_MISMATCH` 存在、`DispatchAttempt.plan_key` 存在且与 config_test_run 的复合检查身份对应、新增字段（envelope `message_id`/`connection_epoch`/`correlation_id`/`boot_id`、`AttemptInputSnapshot`、`ResultPayload`、Agent Model/Tool 消息、Artifact Read/Grep、子执行与 grant 消息、`release_version` 字段）存在；`contracts/quoin/plinth/worker/v1/agent_worker.proto` 必须独立编译并检查 stdio envelope、WorkMode、共享的 ModelInputItemKind/ModelInputRole/ToolExecutionMode/ToolFailureMode、显式最终引用与字段号唯一。（来源：Issue #8「机器契约目录」、Issue #11 交付纪律、Issue #12 前置审计）
- **RUNTIME-VALIDATION-002 —** 协议交错用例 **MUST** 登记并覆盖：握手拒绝矩阵（令牌/slot/版本/epoch/catalog digest）、派发→Accept/Reject、同 boot 重连调和、新 boot Interrupted、结果与取消提交顺序、迟到结果只审计、轮换与替换 fence、上传 commit 的 ABA/epoch 拒绝；浏览器面另覆盖固定 slot FIFO、IdentityBusy 不排队、profile inventory 缺失/manifest 损坏/revision 不符、manual login 单 attachment 与同用户重附着、发布 probe 三态及命令重放、Journey 双 probe、跨多个子 Attempt 的 Exploration Session、可恢复动作失败不关 Session、父终态/lease/new boot 关闭、complete/incomplete trace 与强制 Artifact commit；Stele relay 幂等与 204/4xx/5xx 分类继续覆盖（DATA-TX-016）。（来源：Issue #14、Issue #13）
- **RUNTIME-VALIDATION-003 —** 生产验证 **MUST** 覆盖原型未证明的路径：真实 ingress 的 gRPC 缓冲/keepalive、channel 复用下的流间干扰、noVNC 断线后同用户重附着/第二 attachment 冲突/登出关闭、真实 Chromium profile manifest 与精确 revision 升级失效、Playwright trace 正常/崩溃/incomplete/上传失败、SSO 跨 origin 与 NetworkPolicy 边界、下载阻止、locator/element reference 失效、浏览器进程与 slot 释放；并继续覆盖凭据轮换、grant/Attempt 竞态、Model/Tool/Artifact 与 gRPC 重复投递。（来源：Issue #14、Issue #13、HTTP-VALIDATION-004）
- **RUNTIME-VALIDATION-004 —** proto3 边界校验 **MUST** 在本文件与实现层明确：必填身份/目标 locator（如 attempt、scope、operation、identity、artifact、source、credential）`0`/负数拒绝；`ModelInputItem`/`WorkerModelInputItem` 只有 `item_kind=system_contract|tool_schema` 时 `item_id` 必须为 0，其余封闭 kind（含 `attempt_input_snapshot`）必须为对应权威行 ID 且 >=1；`role` 必须非 `UNSPECIFIED` 并与来源类型一致；明确声明为可选引用的 locator 允许 `0 = 无`（`DispatchAttempt.requested_by_tool_call_id`、`AttemptProgress.tool_call_id`、被拒绝的 `BrowserSubExecutionAck.tool_call_id`/`child_attempt_id`），但负数仍拒绝，条件要求存在时必须 `>= 1`；必填 `uint64` 字段 `0` 拒绝（`generation`/`connection_epoch`/`size_bytes`/`snapshot_version` 等），明确允许无关联的 `correlation_id` 可为 0；`string` 必填字段空串拒绝；`DispatchAttempt` 为 config_test_run 子 Attempt 时 `plan_key` 与 `check_key` 都必须非空，其它 scope 的 `plan_key` 必须为空；`bytes` digest 长度必须 32，唯 `BeginModelCall(operation=EMBEDDING)` 的 `prompt_digest`/`tool_schema_digest` 必须为空；枚举 `*_UNSPECIFIED=0` 值在请求中拒绝；`release_version` 非空；`oneof` 互斥由 proto3 保证（wire 层重复字段按 last-one-wins 处理，接收方 **MUST** 拒绝无法判定的畸形消息）。wire fixture（Go/Node 构造与解码）**MUST** 覆盖全部上述负例与各 finding 对应消息形态（旧流迟到 `AttemptAccept`/`CancelAck` 的 envelope epoch/boot 不匹配、输入/grant 快照、子执行请求/应答、Stele 版本、token 编码与 generation、新 boot 浏览器会话释放）。（来源：Issue #11 第一轮与第二轮审阅）
