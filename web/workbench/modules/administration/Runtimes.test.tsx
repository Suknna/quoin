import "@testing-library/jest-dom/vitest";
import { fireEvent, render, screen, waitFor } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";
import { Runtimes } from "./Runtimes";

const runtime = { slot: "plinth", state: "registered", currentGeneration: 2, pendingGeneration: 3, retiringGeneration: 1, retirementState: "PendingRetirement", rowVersion: 8, connected: true };
describe("Runtimes", () => { it("uses the registration prepare route and expected row-version fence", async () => { const fetchMock = vi.fn().mockResolvedValue({ ok: true, json: async () => ({ plinth: runtime, lintel: { ...runtime, slot: "lintel" } }) }); vi.stubGlobal("fetch", fetchMock); render(<Runtimes suspended={false} />); await screen.findAllByRole("button", { name: "准备替代注册" }); fireEvent.click(screen.getAllByRole("button", { name: "准备替代注册" })[0]); await waitFor(() => expect(fetchMock).toHaveBeenCalledTimes(2)); expect(fetchMock.mock.calls[1][0]).toBe("/api/v1/runtime-slots/plinth/registration/prepare"); expect(JSON.parse(fetchMock.mock.calls[1][1].body)).toMatchObject({ expectedRowVersion: 8 }); }); it("does not restore an asynchronously revealed secret after suspension", async () => { let reveal: (response: unknown) => void = () => {}; const fetchMock = vi.fn().mockImplementation((path: string) => path === "/api/v1/runtime" ? Promise.resolve({ ok: true, json: async () => ({ plinth: runtime, lintel: { ...runtime, slot: "lintel" } }) }) : path.includes("prepare") ? Promise.resolve({ ok: true, json: async () => ({ registrationTokenAvailable: true, registrationTokenHandle: "x".repeat(32) }) }) : new Promise(resolve => { reveal = resolve; })); vi.stubGlobal("fetch", fetchMock); const view = render(<Runtimes suspended={false} />); await screen.findAllByRole("button", { name: "准备替代注册" }); fireEvent.click(screen.getAllByRole("button", { name: "准备替代注册" })[0]); view.rerender(<Runtimes suspended />); reveal({ ok: true, json: async () => ({ registrationToken: "secret" }) }); await waitFor(() => expect(fetchMock).toHaveBeenCalledTimes(3)); expect(screen.queryByText(/一次性注册令牌/)).not.toBeInTheDocument(); }); });
